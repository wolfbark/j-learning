spring.application.name=cqrs-library

# Local run (the tests use Testcontainers instead — see README):
#   docker run --rm -e POSTGRES_USER=lending -e POSTGRES_PASSWORD=lending \
#     -e POSTGRES_DB=lending -p 5432:5432 postgres:16-alpine
spring.datasource.url=jdbc:postgresql://localhost:5432/lending
spring.datasource.username=lending
spring.datasource.password=lending
