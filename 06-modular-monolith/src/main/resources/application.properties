spring.application.name=split-ready-library

# H2 in-memory: the database is not this lesson's topic. Hibernate creates the
# schema on startup — including the event_publication table once the JPA-backed
# event publication registry is in play (step 6).
spring.jpa.open-in-view=false
