# The frozen corpus copied into src/test/resources — five real files from this repository, so
# checkpoint assertions never move when someone edits a lesson.
rag.corpus-paths=classpath*:corpus/*.md
rag.top-k=4
rag.similarity-threshold=0.0
spring.flyway.enabled=true
logging.level.org.springframework.ai=WARN
