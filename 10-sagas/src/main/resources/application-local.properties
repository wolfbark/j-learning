# For manual runs against `docker compose up -d`:
#   mvn spring-boot:run -Dspring-boot.run.profiles=local,choreography
spring.datasource.url=jdbc:postgresql://localhost:5432/trips
spring.datasource.username=trips
spring.datasource.password=trips
spring.kafka.bootstrap-servers=localhost:9092
